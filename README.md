# LangChain Codes
# LangChain Learning Journey 🦜🔗

This repository contains my code, experiments, and practice work while learning **LangChain** and building applications with LLMs.

The goal of this repository is to learn LangChain step by step — starting from the fundamentals and gradually moving toward more advanced concepts such as prompts, chat models, chains, agents, RAG, vector databases, and AI application development.

## 📚 What I'm Learning

* LangChain fundamentals
* Chat Models & LLMs
* Prompt Templates
* Output Parsers
* Chains & LCEL
* Document Loaders
* Text Splitters
* Embeddings
* Vector Databases
* Retrieval-Augmented Generation (RAG)
* Agents & Tools
* Memory
* LangGraph
* Building LLM-powered applications

## 📂 Repository Structure

```text
Langchain_codes/
│
├── langchain/
│   ├── chatmodels/
│   ├── prompts/
│   ├── chains/
│   ├── agents/
│   ├── rag/
│   └── ...
│
├── pydantic/
│
├── requirements.txt
├── README.md
└── .gitignore
```

> The structure will evolve as I progress through different LangChain concepts.

## 🛠️ Tech Stack

* **Python**
* **LangChain**
* **OpenAI / LLM APIs**
* **Pydantic**
* **Python-dotenv**
* **Vector Databases**
* **LangGraph**

## 🚀 Setup

Clone the repository:

```bash
git clone https://github.com/pavithrasunilkumar/Langchain_codes.git
cd Langchain_codes
```

Create a virtual environment:

```bash
python -m venv .venv
```

Activate it on Windows:

```powershell
.venv\Scripts\activate
```

Install the dependencies:

```bash
pip install -r requirements.txt
```

## 🔐 Environment Variables

Create a `.env` file in the project directory and add the required API keys:

```env
OPENAI_API_KEY=your_api_key_here
```

**Never commit your `.env` file or expose API keys publicly.**

## 🎯 Learning Goal

This repository is a record of my progress in understanding how modern LLM applications are built using LangChain.

I'll continue adding examples and experiments as I learn new concepts.

---

**Learning → Experimenting → Building → Improving 🚀**
